#include <bits/stdc++.h>

using namespace std;

int main() {

    // map<int, int> m;

    // for (int i = 0; i < 10; i++)
    //     m[i] = 2 * i;

    // m.erase(1);

    // for (auto i : m)
    //     cout << i.first << " :" << i.second << endl;
    int n = 10;
    cout << n << endl;
}